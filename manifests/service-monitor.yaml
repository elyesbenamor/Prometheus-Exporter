apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: bull_exporter_monitor
  labels:
    app: bull
    role: exporter
spec:
  selector:
    matchLabels:
      app: bull
      role: exporter
  endpoints:
  - port: http
    interval: 15s
